import styled from 'styled-components';

export const StyledMainLabel = styled.label`
    display: flex;
    flex-direction: column;
    margin: .5rem;

    & > span {
        margin-bottom: .4rem
    }
`;
